package dayXX

@main
def part1(path: String): String =
  val input = os.read(os.pwd / "inputs" / "$path.txt")

  return ""

@main
def part2(path: String): String =
  val input = os.read(os.pwd / "inputs" / "$path.txt")

  return ""
